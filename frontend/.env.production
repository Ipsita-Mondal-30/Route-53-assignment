# Browser-facing API base. On Vercel this MUST be the same-origin proxy.
NEXT_PUBLIC_API_URL=/api/backend
# Server-only upstream used by src/app/api/backend/[...path]
API_URL=https://route-53-assignment.onrender.com
