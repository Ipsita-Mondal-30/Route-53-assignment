import { PlaceholderPage } from "@/components/console/placeholder-page";

export default function HealthChecksPage() {
  return (
    <PlaceholderPage
      title="Health checks"
      description="Create health checks to monitor endpoints and route DNS queries to healthy resources. Health check configuration will connect to the backend later."
    />
  );
}
